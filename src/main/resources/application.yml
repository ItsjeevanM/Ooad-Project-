spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edusphere_lms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: jeeva
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  jwt:
    secret: edusphere-super-secret-key-must-be-32-chars-long
    expiry-ms: 86400000

server:
  port: 8081